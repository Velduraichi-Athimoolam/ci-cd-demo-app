apiVersion: v2
name: ci-cd-demo
description: A Helm chart for CI/CD demo application (Flask + Docker + Kubernetes)

type: application

version: 0.1.0        # Chart version (increment when chart changes)
appVersion: "1.0.0"   # Application version (matches your app/Docker tag)